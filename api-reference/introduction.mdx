---
title: 'API Reference'
description: 'Complete API reference for Valour - a comprehensive financial services platform'
---

## Welcome to the Valour API

The Valour API provides a comprehensive suite of financial services including KYC, payments, virtual accounts, and compliance solutions. This API reference contains detailed documentation for all available endpoints.

<Card
  title="OpenAPI Specification"
  icon="code"
  href="/api-reference/openapi.json"
>
  View the complete OpenAPI specification file
</Card>

## Authentication

The Valour API supports multiple authentication methods:

### API Key Authentication
Include your API key in the `X-API-Key` header:

```bash
curl -H "X-API-Key: your-api-key-here" https://api.railsfromthecrypt.com/v1/endpoint
```

### Bearer Token Authentication
Use JWT tokens for authenticated requests:

```bash
curl -H "Authorization: Bearer your-jwt-token" https://api.railsfromthecrypt.com/v1/endpoint
```

## Base URLs

- **Production**: `https://api.railsfromthecrypt.com/`
- **Staging**: `https://staging-api.railsfromthecrypt.com/`

## Rate Limiting

API requests are rate limited to ensure fair usage. Rate limits are applied per API key and vary by endpoint type.

## Error Handling

All API responses follow a consistent error format:

```json
{
  "message": "Error description",
  "error": {
    "code": "ERROR_CODE",
    "type": "error_type"
  }
}
```

## Getting Started

1. **Get your API key** from the [dashboard](https://dashboard.railsfromthecrypt.com)
2. **Start with authentication** using the `/v1/auth/login` endpoint
3. **Explore the endpoints** organized by category in the navigation

<CardGroup cols={2}>
  <Card
    title="Authentication"
    icon="key"
    href="/api/auth/post-auth-login"
  >
    Learn how to authenticate with the API
  </Card>
  <Card
    title="Quick Start"
    icon="rocket"
    href="/quickstart"
  >
    Get up and running quickly
  </Card>
</CardGroup>
